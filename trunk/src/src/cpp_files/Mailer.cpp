/*
 * Mailer.cpp
 *
 *  Created on: Jun 17, 2009
 *      Author: dikla
 */
#include "Mailer.h"
/*
	 Mailer::Mailer(Shell& shell, int numOfWorkers, int bfrSize){
`		_shell=shell;
		_numOfWorkers=numOfWorkers;
		_inboxSize=bfrSize;
	 }

	 virtual ~Mailer(){}

	 //***********************************************
	 Mailer::voidrun(){}
	 Mailer::void start(){}
	 Mailer::void setScheduler(Scheduler* s){}



	 //****************handle messages***********************
	 Mailer::bool setMsgInInbox(Message* m){}//if inbox full return false
	// Reads the messages from the mailbox to msgQ
	 Mailer::bool receive(int mailboxID, std::queue<Message*>& msgQ){}
	 Mailer::Message* readMsgFromInbox(){}
	 Mailer::void setMsgInProgress(bool en){}//_noMsgInProgress==true or false

	//****************handle workers***********************
	 Mailer::Worker& getWorker(int wID){}
	 Mailer::void setWorkerOff(int wID){}
	 Mailer::void setWorkerOn(int wID){}
	 Mailer::void addWorker(Worker* worker){}
	 Mailer::Mailbox& getMailbox(int mailboxID){}
	 Mailer::void reInitWorkers(int workerID){}

*/
